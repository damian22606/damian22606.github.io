'use strict';

for (var i=4 ; i>0 ; i-- ){
    console.log(i); 
}

var tablica =["Krystian", "Monika", "Danuta"] ;
 tablica.foreach (function(element, index) {
                  console.log("Element z indexem" + index + "ma wartosc " + element) ;}
                  )
for var i=0; i<tablica.lengh; i++ {
    console.log("Element z indexem" + i + "ma wartosc " + ) ;}
                  )
}


